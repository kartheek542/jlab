name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
    - name: step 1
      run: echo "this is step 1"
    - name: step 2
      run : echo "this is step 2"
